package io.accelerate.solutions.CHL

import io.accelerate.runner.SolutionNotImplementedException

object Checklite {
  def checklite(skus: String): Integer = throw new SolutionNotImplementedException()
}
