package io.accelerate.solutions.ULT

import io.accelerate.runner.SolutionNotImplementedException

import java.util

class UltimateSolution {
  def ultimateMaze(rows: Int, columns: Int, mazeGenerationOptions: util.Map[String, String]): String = throw new SolutionNotImplementedException
}
